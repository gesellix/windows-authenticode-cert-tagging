---
name: CI
on:
  - workflow_dispatch
  - push

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
      - name: Check GoReleaser config
        uses: goreleaser/goreleaser-action@v5
        with:
          version: latest
          args: check
      - name: Check
        run: go test -v ./...
      - name: Build
        run: go build -o certificate_tag -v ./cmd/certificate_tag.go
      - name: Archive artifact
        uses: actions/upload-artifact@v4
        with:
          name: certificate_tag-linux
          path: ./certificate_tag
...
